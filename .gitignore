node_modules/
dist/
.env
oauth_credentials.json
www/
tag-results.json
*.code-workspace

# Throwaway visual mockups for mobile UI direction choice
mobile-mockups/

# IDE
.idea/
android/.idea/

# Capacitor sync artifacts (regenerated by `npx cap sync`)
android/app/capacitor.build.gradle
android/capacitor.settings.gradle
.venv/

# Claude Code — outillage local (settings, skills, sessions d'audit).
# Exception : on peut commit .claude/agents/ si on veut partager des
# agents custom avec le repo (fait sur la branch mobile-refonte).
.claude/
!.claude/agents/
